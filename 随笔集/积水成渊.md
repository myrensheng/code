### python

```python
# dict中的get函数的使用
"""
Python 字典(Dictionary) get(key,default=None) 
函数返回指定键的值，如果值不在字典中返回默认值。
例如在word_count文件中的result.get(i,0),如果i在result中则返回i对应的值，如果i不在result中则返回的是默认值0，
"""

```

### 单词积累

```python 
# matrix 矩阵 在numpy中创建一个矩阵使用：numpy.matrix([[1,2],[3,4],[5,6]])
# array 数组 在python中的array使用的* 表示的是：Hadamard乘积对应元素相乘
# inverse matrix 逆矩阵
# transpose 转置
```

